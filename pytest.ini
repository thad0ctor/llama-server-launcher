[pytest]
# ---------------------------------------------------------------------------
# pytest configuration for llama-server-launcher
# ---------------------------------------------------------------------------
#
# ``-ra`` shows the summary section for skipped/xfailed tests.
addopts = -ra
